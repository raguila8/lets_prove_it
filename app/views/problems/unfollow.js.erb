$('#problem-follow-<%=@problem.id%>').html("<a href='<%=problem_follow_path(@problem.id)%>' data-remote='true' data-method='post'><button class='btn btn-light'>Follow</button></a>");
$('#problem-<%=@problem.id%>-followers').text('<%= pluralize(@problem.followers.count, "Follower") %>');
