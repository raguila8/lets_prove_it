$('#problem-follow-<%=@problem.id%>').html("<a href='<%=problem_unfollow_path(@problem.id)%>' data-remote='true' data-method='delete'><button class='btn btn-light'>Following</button></a>");

$('#problem-<%=@problem.id%>-followers').text('<%= pluralize(@problem.followers.count, "Follower") %>');
