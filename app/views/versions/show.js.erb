$("#collapse-<%=@version.id%> .panel-body").append("<%= escape_javascript render(:partial => '/versions/content', locals:  {version: @version} ) %>");

$content = $("#collapse-<%=@version.id%> .panel-body");
  setTimeout(function() {
	  MathJax.Hub.Queue(["Typeset", MathJax.Hub, $content[0]]);
} ,500);

$content.find(".spinner").slideUp(1000);
$content.data("has-content", "true");


