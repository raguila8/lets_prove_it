$('#content1').html("<%= escape_javascript render(:partial => '/problems/profile_stream_item', collection:  @problems, as: :problem ) %>");

$content = $('#content1');
  setTimeout(function() {
	  MathJax.Hub.Queue(["Typeset", MathJax.Hub, $content[0]]);
} ,500);

$('.spinner').slideUp(3000);

