$('#content3').html("<%= escape_javascript render(:partial => '/shared/items/comment', collection:  @comments, as: :comment ) %>");

$content = $('#content3');
  setTimeout(function() {
	  MathJax.Hub.Queue(["Typeset", MathJax.Hub, $content[0]]);
} ,500);

$('.spinner').slideUp(3000);
$('#content3').attr("data-loaded", true);



