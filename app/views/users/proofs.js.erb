$('#content2').html("<%= escape_javascript render(:partial => '/proofs/profile_proof_item', collection:  @proofs, as: :proof ) %>");

$content = $('#content2');
  setTimeout(function() {
	  MathJax.Hub.Queue(["Typeset", MathJax.Hub, $content[0]]);
} ,500);

$('.spinner').slideUp(3000);
$('#content2').attr("data-loaded", true);




/*
$('.tab-content .boxedtitle h2').text("Proofs");
$('.tab-content .tab-list-content').html("<%#= escape_javascript render(:partial => '/shared/items/proof_item', collection:  @proofs, as: :model ) %>");

$content = $('.tab-content');
  setTimeout(function() {
	  MathJax.Hub.Queue(["Typeset", MathJax.Hub, $content[0]]);
} ,500);
*/
