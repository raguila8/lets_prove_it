$('.tab-content .boxedtitle h2').text("Problem Edits");
$('.tab-content .tab-list-content').empty();
$('.tab-content .tab-list-content').html("<div class='accordions-1'></div>");
$('.tab-content .tab-list-content').append("<%= escape_javascript render(:partial => '/shared/items/version_item', collection:  @versions, as: :model ) %>");

$content = $('.tab-content');
  setTimeout(function() {
	  MathJax.Hub.Queue(["Typeset", MathJax.Hub, $content[0]]);
} ,500);

$('.spinner').slideUp(3000);

