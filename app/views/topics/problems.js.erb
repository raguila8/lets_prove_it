$('.topic-content').empty();
<% if @problems.count > 0 %>
  <% @problems.each do |problem| %>
    $('.topic-content').append("<%= escape_javascript render(:partial => '/problems/problem_post', :locals => { :@problem => @problem }) %>");
  <% end %>
<% else %>
  
<% end %>
